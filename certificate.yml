apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dev-frontend-cert
  namespace: thetym
spec:
  secretName: dev-frontend-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - devnew.thetym.com
