apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: thetym
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: dinesh10275/dev-frontend:v1
        ports:
        - containerPort: 80
        
